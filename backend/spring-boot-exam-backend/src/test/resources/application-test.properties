# JUnit 테스트시
# 테스트 클래스에 @ActiveProfiles("abc") 빈 설정을 통해
#    -> application-{"abc"}.properties 사용설정
# DB 정보 가리려고  application.properties 에 프로젝트 환경변수로 교체 했더니 테스트에서 못읽어서 처리

# ??
spring.application.name=spring-boot-exam-backend
logging.level.org.springframework=info
logging.level.org.mbc=debug

# ??
server.port=0

# DB ??
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://192.168.0.171:33062/bootexamdb
spring.datasource.username=bootexam
spring.datasource.password=bootexam

# ORM ?? (Spring Data JPA) 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true